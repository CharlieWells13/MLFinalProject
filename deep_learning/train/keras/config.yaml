data_dir: data
epochs: 20
batch_size: 32
lr: 0.001
weight_decay: 0.0001
optimizer:
  # options: adamw, adam, sgd
  name: adamw
  # optimizer-specific kwargs (example: momentum for sgd, beta_1/beta_2/epsilon for adam/adamw)
  params: {}
loss:
  # options: huber, mse, mae
  name: huber
  # loss-specific kwargs (example: delta for huber)
  params: {}
image_size: 224
freeze_backbone: false
output: deep_learning/checkpoints/model_keras_best.keras

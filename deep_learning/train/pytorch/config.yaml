data_dir: data
epochs: 20
batch_size: 32
lr: 0.001
weight_decay: 0.0001
optimizer:
  # options: adamw, adam, sgd
  name: adamw
  # optimizer-specific kwargs (example: momentum for sgd, betas/eps for adam/adamw)
  params: {}
loss:
  # options: smooth_l1, huber, mse, l1
  name: smooth_l1
  # loss-specific kwargs (example: delta for huber, beta for smooth_l1)
  params: {}
image_size: 224
freeze_backbone: false
num_workers: 2
output: deep_learning/checkpoints/model_pytorch_best.pt
